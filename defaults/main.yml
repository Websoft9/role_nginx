# port for java/nodejs...  8080 | 3000
nginx_reverse_proxy_port:

# reverse | alias | www | 
nginx_vhost_mode: "www"

# default listen port of default.conf
nginx_listen_port: "80"

# nginx username and password swith True|False
# default credentials is admin/admin
nginx_htpasswd: False

nginx_certbot: True
